{
  "annotations": {
    "list": [
      {
        "builtIn": 1,
        "datasource": "-- Grafana --",
        "enable": true,
        "hide": true,
        "iconColor": "rgba(0, 211, 255, 1)",
        "name": "Annotations & Alerts",
        "type": "dashboard"
      }
    ]
  },
  "editable": true,
  "graphTooltip": 0,
  "panels": [
    {
      "datasource": "Prometheus",
      "fieldConfig": {"defaults": {"unit": "none"}},
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0},
      "id": 1,
      "targets": [{"expr": "arl_shift_score", "legendFormat": "shift_score"}],
      "title": "Shift Score",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": {"defaults": {"unit": "none"}},
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0},
      "id": 2,
      "targets": [{"expr": "arl_risk_capital", "legendFormat": "risk_capital"}],
      "title": "Risk Capital",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": {"defaults": {"unit": "percentunit"}},
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 8},
      "id": 3,
      "targets": [{"expr": "arl_batch_accuracy", "legendFormat": "batch_accuracy"}],
      "title": "Batch Accuracy",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": {"defaults": {"unit": "ops"}},
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 8},
      "id": 4,
      "targets": [{"expr": "rate(arl_interventions_total[5m])", "legendFormat": "{{action}} {{mode}}"}],
      "title": "Intervention Rate",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": {"defaults": {"unit": "ops"}},
      "gridPos": {"h": 8, "w": 24, "x": 0, "y": 16},
      "id": 5,
      "targets": [{"expr": "rate(arl_risk_alerts_total[5m])", "legendFormat": "risk_alerts"}],
      "title": "Risk Alerts",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 39,
  "tags": ["adaptive-reliability-layer", "ml-ops"],
  "time": {"from": "now-6h", "to": "now"},
  "title": "Adaptive Reliability Layer",
  "uid": "arl-runtime",
  "version": 1
}
